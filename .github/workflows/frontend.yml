name: Front-End Deployment

env:
  AZURE_WEBAPP_NAME: pipeline-test-git   # set this to your function app name on Azure
  AZURE_WEBAPP_PACKAGE_PATH: '.'       # set this to the path to your function app project, defaults to the repository root
  DOTNET_VERSION: '6.0.x'   
  
on:
#   push:
#     branches:
    workflow_run:
      workflows: ["Azure Web App Deployment"]
      types:
        - completed

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Node.js and Build Frontend
        run: |
          cd frontend
          npm install
          npm run build
        shell: bash

  deploy-to-azure:
    runs-on: ubuntu-latest

    needs: build-and-deploy

    steps:
      - name: Deploy to Azure Web App (Frontend)
        shell: bash
        run: |
          cd frontend
          working-directory: frontend
          azure/webapps-deploy@v2:
            app-name: pipeline-test-git
            publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
